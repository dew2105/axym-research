# MotherDuck (hosted DuckDB)
MOTHERDUCK_TOKEN=your_motherduck_token_here
MOTHERDUCK_DB=axym_research

# Neon (serverless PostgreSQL) — full connection string with sslmode=require
POSTGRES_DSN=postgresql://user:password@ep-xxxxx.us-east-2.aws.neon.tech/axym_research?sslmode=require

# Benchmark
BENCHMARK_RUNS=3

# Data directory (relative to repo root)
DATA_DIR=data
RESULTS_DIR=results
